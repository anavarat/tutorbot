{
  "name": "@tutorbot/platform-gateway-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "deploy": "pnpm --dir ../../../packages/shared build && wrangler deploy",
    "test": "pnpm --dir ../../../packages/shared build && vitest run",
    "typecheck": "pnpm exec tsc -p tsconfig.json",
    "validate:config": "mkdir -p .wrangler/types && wrangler types .wrangler/types/default.d.ts --config wrangler.jsonc && wrangler types .wrangler/types/staging.d.ts --config wrangler.jsonc --env staging && wrangler types .wrangler/types/us-production.d.ts --config wrangler.jsonc --env us-production && wrangler types .wrangler/types/uk-production.d.ts --config wrangler.jsonc --env uk-production && wrangler types .wrangler/types/au-production.d.ts --config wrangler.jsonc --env au-production"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7",
    "@tutorbot/shared": "workspace:*",
    "hono": "4.6.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260623.1",
    "@types/node": "22.10.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8",
    "wrangler": "4.104.0"
  }
}

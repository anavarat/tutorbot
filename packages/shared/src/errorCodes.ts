export const CONTAINER_REQUEST_FAILED = "CONTAINER_REQUEST_FAILED";

/** Requested gatewayId is not in the Worker's GATEWAY_IDS allowlist. */
export const GATEWAY_UNKNOWN = "GATEWAY_UNKNOWN";

/** Request body failed basic shape/JSON validation at the Worker edge. */
export const INVALID_REQUEST = "INVALID_REQUEST";
